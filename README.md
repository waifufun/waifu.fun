# af-monorepo

sudo apt-get update && sudo apt-get install -y \
  libvips-dev \
  build-essential \
  pkg-config \
  libjpeg-dev \
  libpng-dev \
  libtiff-dev \
  libwebp-dev

sudo apt-get update && sudo apt-get install -y \
  libvips-dev build-essential \
  pkg-config libjpeg-dev libpng-dev libtiff-dev libwebp-dev