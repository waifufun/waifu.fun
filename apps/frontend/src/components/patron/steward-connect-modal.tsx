"use client";

/**
 * Stub. The full two-paths modal lands in W7.3 commit 2.
 * Kept here so the badge import resolves cleanly in commit 1.
 */
type Props = {
	open: boolean;
	onOpenChange: (open: boolean) => void;
};

export default function StewardConnectModal(_props: Props) {
	return null;
}
