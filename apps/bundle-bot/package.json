{
	"name": "@waifufun/bundle-bot",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"predev": "bun run --filter @waifufun/config build && bun run --filter @waifufun/logger build && bun run --filter @waifufun/db build",
		"prestart": "bun run --filter @waifufun/config build && bun run --filter @waifufun/logger build && bun run --filter @waifufun/db build",
		"prebuild": "bun run --filter @waifufun/config build && bun run --filter @waifufun/logger build && bun run --filter @waifufun/db build",
		"start": "tsx src/index.ts",
		"dev": "tsx watch src/index.ts",
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "tsx --test --test-reporter=spec --test-concurrency=1 $(find src -name '*.test.ts')",
		"lint": "biome check src package.json tsconfig.json",
		"check": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@waifufun/config": "workspace:*",
		"@waifufun/db": "workspace:*",
		"@waifufun/logger": "workspace:*",
		"drizzle-orm": "^0.36.4",
		"postgres": "^3.4.9",
		"viem": "^2.21.54"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"tsx": "^4.19.2",
		"typescript": "^5.7.2"
	}
}
