{
	"name": "@waifufun/brain",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"prebuild": "bun run --filter @waifufun/db build && bun run --filter @waifufun/logger build",
		"predev": "bun run --filter @waifufun/db build && bun run --filter @waifufun/logger build",
		"prestart": "bun run --filter @waifufun/db build && bun run --filter @waifufun/logger build",
		"pretypecheck": "bun run --filter @waifufun/db build && bun run --filter @waifufun/logger build",
		"dev": "tsx watch src/index.ts",
		"start": "tsx src/index.ts",
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"lint": "biome check src package.json tsconfig.json",
		"test": "sh -c 'files=$(find test -name \"*.test.ts\" -type f | sort); node --import tsx --test $files'",
		"check": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@waifufun/db": "workspace:*",
		"@waifufun/logger": "workspace:*",
		"twitter-api-v2": "^1.18.2"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"tsx": "^4.19.2",
		"typescript": "^5.7.2"
	}
}
